services:
  - type: web
    name: flask-trading-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    envVars:
      - key: UPSTOX_ACCESS_TOKEN
        sync: false